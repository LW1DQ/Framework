╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║                   📦 CÓMO EXPORTAR EL SISTEMA A2A v1.2                      ║
║                                                                              ║
║                    Guía para Transferir a Otra Máquina                      ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝

┌──────────────────────────────────────────────────────────────────────────────┐
│                        📋 MÉTODOS DE EXPORTACIÓN                             │
└──────────────────────────────────────────────────────────────────────────────┘

  Este documento explica cómo exportar el framework completo para instalarlo
  en otra máquina.

┌──────────────────────────────────────────────────────────────────────────────┐
│                    MÉTODO 1: Archivo Comprimido (Recomendado)               │
└──────────────────────────────────────────────────────────────────────────────┘

  Crear archivo .tar.gz (Linux/Mac):
  ───────────────────────────────────

    # Desde el directorio padre
    cd /ruta/donde/esta/sistema-a2a-export
    cd ..

    # Comprimir todo el directorio
    tar -czf sistema-a2a-v1.2.tar.gz sistema-a2a-export/

    # Verificar tamaño
    ls -lh sistema-a2a-v1.2.tar.gz

    Tamaño esperado: ~50-100 MB (sin venv)


  Crear archivo .zip (Windows):
  ──────────────────────────────

    # Usando PowerShell
    Compress-Archive -Path sistema-a2a-export -DestinationPath sistema-a2a-v1.2.zip

    # O usar interfaz gráfica:
    # Click derecho > Enviar a > Carpeta comprimida


  Transferir el archivo:
  ───────────────────────

    Opción A - USB/Disco externo:
      Copiar sistema-a2a-v1.2.tar.gz al USB

    Opción B - Red/Email:
      Enviar por email o subir a Google Drive/Dropbox

    Opción C - Servidor:
      scp sistema-a2a-v1.2.tar.gz usuario@servidor:/ruta/destino/


  Descomprimir en máquina nueva:
  ───────────────────────────────

    # Linux/Mac
    tar -xzf sistema-a2a-v1.2.tar.gz
    cd sistema-a2a-export
    ./install.sh

    # Windows (PowerShell)
    Expand-Archive sistema-a2a-v1.2.zip -DestinationPath .
    cd sistema-a2a-export

┌──────────────────────────────────────────────────────────────────────────────┐
│                    MÉTODO 2: Copia Directa (USB/Disco)                      │
└──────────────────────────────────────────────────────────────────────────────┘

  Copiar a USB:
  ─────────────

    # Linux/Mac
    cp -r sistema-a2a-export /media/usb/

    # Windows
    xcopy sistema-a2a-export E:\sistema-a2a-export /E /I


  Copiar desde USB a máquina nueva:
  ──────────────────────────────────

    # Linux/Mac
    cp -r /media/usb/sistema-a2a-export ~/sistema-a2a
    cd ~/sistema-a2a
    ./install.sh

    # Windows
    xcopy E:\sistema-a2a-export C:\Users\Usuario\sistema-a2a /E /I

┌──────────────────────────────────────────────────────────────────────────────┐
│                    MÉTODO 3: Transferencia por Red (SCP)                    │
└──────────────────────────────────────────────────────────────────────────────┘

  Desde máquina origen:
  ─────────────────────

    # Comprimir primero (recomendado)
    tar -czf sistema-a2a-v1.2.tar.gz sistema-a2a-export/

    # Transferir a máquina destino
    scp sistema-a2a-v1.2.tar.gz usuario@maquina-destino:~/


  En máquina destino:
  ───────────────────

    # Descomprimir
    tar -xzf sistema-a2a-v1.2.tar.gz
    cd sistema-a2a-export

    # Instalar
    ./install.sh

┌──────────────────────────────────────────────────────────────────────────────┐
│                    MÉTODO 4: Repositorio Git (Avanzado)                     │
└──────────────────────────────────────────────────────────────────────────────┘

  Crear repositorio:
  ──────────────────

    cd sistema-a2a-export
    git init
    git add .
    git commit -m "Sistema A2A v1.2 - Exportación inicial"

    # Subir a GitHub/GitLab
    git remote add origin https://github.com/tu-usuario/sistema-a2a.git
    git push -u origin main


  Clonar en máquina nueva:
  ─────────────────────────

    git clone https://github.com/tu-usuario/sistema-a2a.git
    cd sistema-a2a
    ./install.sh

┌──────────────────────────────────────────────────────────────────────────────┐
│                    ⚠️  IMPORTANTE: QUÉ NO EXPORTAR                          │
└──────────────────────────────────────────────────────────────────────────────┘

  Excluir estos directorios (se crean automáticamente):
  ──────────────────────────────────────────────────────

    ❌ venv/                    # Entorno virtual (recrear en destino)
    ❌ __pycache__/             # Cache de Python
    ❌ *.pyc                    # Archivos compilados
    ❌ logs/*.log               # Logs antiguos
    ❌ data/vector_db/*         # Base de datos (opcional)
    ❌ simulations/results/*    # Resultados antiguos (opcional)
    ❌ simulations/plots/*      # Gráficos antiguos (opcional)


  Comando para comprimir excluyendo estos directorios:
  ─────────────────────────────────────────────────────

    tar -czf sistema-a2a-v1.2.tar.gz \
        --exclude='venv' \
        --exclude='__pycache__' \
        --exclude='*.pyc' \
        --exclude='logs/*.log' \
        --exclude='data/vector_db/*' \
        --exclude='simulations/results/*' \
        --exclude='simulations/plots/*' \
        sistema-a2a-export/

    Esto reduce significativamente el tamaño del archivo.

┌──────────────────────────────────────────────────────────────────────────────┐
│                    ✅ CHECKLIST ANTES DE EXPORTAR                            │
└──────────────────────────────────────────────────────────────────────────────┘

  Verificar que el paquete incluya:

  □ Todos los archivos de código fuente (agents/, config/, utils/)
  □ Documentación completa (docs/, *.md)
  □ Scripts de instalación (install.sh, scripts/)
  □ Ejemplos funcionales (examples/)
  □ Tests (tests/)
  □ requirements.txt actualizado
  □ .gitignore configurado
  □ LEEME-EXPORTACION.txt
  □ INSTALACION-COMPLETA.md
  □ COMO-EXPORTAR.txt (este archivo)

  Verificar que NO incluya:

  □ venv/ (entorno virtual)
  □ __pycache__/ (cache de Python)
  □ *.pyc (archivos compilados)
  □ Logs antiguos innecesarios
  □ Resultados de simulaciones antiguas (opcional)
  □ Datos sensibles o personales

┌──────────────────────────────────────────────────────────────────────────────┐
│                    📊 TAMAÑOS ESPERADOS                                      │
└──────────────────────────────────────────────────────────────────────────────┘

  Paquete completo (sin venv, sin resultados):
    • Sin comprimir: ~20-30 MB
    • Comprimido (.tar.gz): ~5-10 MB
    • Comprimido (.zip): ~8-15 MB

  Paquete con resultados de ejemplo:
    • Sin comprimir: ~50-100 MB
    • Comprimido: ~20-40 MB

  Si el tamaño es mucho mayor:
    • Verificar que no incluya venv/
    • Verificar que no incluya resultados antiguos
    • Limpiar __pycache__/ y *.pyc

┌──────────────────────────────────────────────────────────────────────────────┐
│                    🔍 VERIFICACIÓN POST-EXPORTACIÓN                          │
└──────────────────────────────────────────────────────────────────────────────┘

  Después de exportar, verificar en máquina nueva:

  1. Descomprimir/copiar el paquete
  2. Verificar estructura de directorios:
     $ ls -la sistema-a2a-export/

  3. Verificar archivos principales:
     $ ls sistema-a2a-export/*.py
     $ ls sistema-a2a-export/agents/
     $ ls sistema-a2a-export/docs/

  4. Ejecutar instalación:
     $ cd sistema-a2a-export
     $ ./install.sh

  5. Verificar sistema:
     $ source venv/bin/activate
     $ python scripts/check_system.py

  6. Probar ejemplo:
     $ python examples/ejemplo_basico.py

┌──────────────────────────────────────────────────────────────────────────────┐
│                    💡 CONSEJOS PARA EXPORTACIÓN                              │
└──────────────────────────────────────────────────────────────────────────────┘

  1. Comprimir siempre
     Reduce tamaño y facilita transferencia

  2. Excluir venv/
     El entorno virtual debe recrearse en destino

  3. Incluir documentación
     Asegúrate de incluir todos los .md

  4. Verificar permisos
     El install.sh debe ser ejecutable (chmod +x)

  5. Probar en máquina limpia
     Verifica que la instalación funcione desde cero

  6. Documentar dependencias externas
     NS-3 y Ollama deben instalarse por separado

  7. Incluir versión
     Nombra el archivo con versión (v1.2)

  8. Crear checksum (opcional)
     Para verificar integridad:
     $ sha256sum sistema-a2a-v1.2.tar.gz > checksum.txt

┌──────────────────────────────────────────────────────────────────────────────┐
│                    🚀 COMANDOS RÁPIDOS DE EXPORTACIÓN                        │
└──────────────────────────────────────────────────────────────────────────────┘

  Exportación completa optimizada:
  ────────────────────────────────

    # 1. Limpiar archivos innecesarios
    cd sistema-a2a-export
    find . -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null
    find . -type f -name "*.pyc" -delete
    rm -rf venv/
    rm -rf logs/*.log

    # 2. Volver al directorio padre
    cd ..

    # 3. Comprimir excluyendo directorios grandes
    tar -czf sistema-a2a-v1.2.tar.gz \
        --exclude='sistema-a2a-export/venv' \
        --exclude='sistema-a2a-export/__pycache__' \
        --exclude='sistema-a2a-export/data/vector_db/*' \
        --exclude='sistema-a2a-export/simulations/results/*' \
        --exclude='sistema-a2a-export/simulations/plots/*' \
        sistema-a2a-export/

    # 4. Verificar
    ls -lh sistema-a2a-v1.2.tar.gz

    # 5. Crear checksum
    sha256sum sistema-a2a-v1.2.tar.gz > sistema-a2a-v1.2.sha256


  Exportación rápida (todo incluido):
  ────────────────────────────────────

    cd ..
    tar -czf sistema-a2a-v1.2-full.tar.gz sistema-a2a-export/

┌──────────────────────────────────────────────────────────────────────────────┐
│                    📝 NOTAS PARA EL DESTINATARIO                             │
└──────────────────────────────────────────────────────────────────────────────┘

  Incluir estas instrucciones con el paquete:

  1. Descomprimir el archivo
  2. Leer LEEME-EXPORTACION.txt primero
  3. Ejecutar ./install.sh para instalación automática
  4. O seguir INSTALACION-COMPLETA.md para instalación manual
  5. Verificar con: python scripts/check_system.py
  6. Probar con: python examples/ejemplo_basico.py

  Requisitos previos:
    • Python 3.8+
    • NS-3 (instalar por separado)
    • Ollama (instalar por separado)
    • 20GB+ de espacio libre

╔══════════════════════════════════════════════════════════════════════════════╗
║                                                                              ║
║                    ✅ LISTO PARA EXPORTAR                                    ║
║                                                                              ║
║              Usa los comandos de arriba para crear el paquete                ║
║                                                                              ║
╚══════════════════════════════════════════════════════════════════════════════╝

Versión: 1.2
Fecha: 2024-11-23
Estado: Producción

¡El paquete está listo para transferir a otra máquina! 📦🚀
